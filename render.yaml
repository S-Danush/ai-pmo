# Optional Render Blueprint: Dashboard → New → Blueprint → paste this repo.
# Or create manually: Dockerfile path = Dockerfile, Docker context = . (repo root).
services:
  - type: web
    name: ai-pmo-agent-api
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: Dockerfile
    dockerContext: .
    healthCheckPath: /api/health
